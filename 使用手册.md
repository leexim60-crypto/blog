# 个人博客系统 - 使用手册

## 目录

1. [系统概述](#1-系统概述)
2. [环境要求](#2-环境要求)
3. [安装部署](#3-安装部署)
4. [功能说明](#4-功能说明)
5. [Markdown 写作指南](#5-markdown-写作指南)
6. [技术架构](#6-技术架构)
7. [常见问题](#7-常见问题)

---

## 1. 系统概述

本博客系统是专为前端工程师打造的个人技术博客，包含三大内容板块：

- **代码** - 编程技术分享、代码片段、项目实战
- **面试八股** - 前端面试题、八股文整理、高频考点
- **感悟** - 技术感悟、成长心得、生活随想

系统支持完整的文章管理功能，包括用户注册登录、文章发布编辑、分类筛选、全文搜索等。

---

## 2. 环境要求

| 软件 | 版本要求 | 说明 |
|------|---------|------|
| Node.js | >= 16.0 | JavaScript运行环境 |
| npm | >= 8.0 | 包管理工具 |
| MySQL | >= 8.0 | 数据库 |

---

## 3. 安装部署

### 3.1 数据库初始化

1. 确保 MySQL 8.0 服务已启动

2. 运行数据库初始化脚本：

```bash
cd blog-server
npm run init-db
```

该脚本会自动完成：
- 创建 `personal_blog` 数据库
- **删除旧表并重建**所需数据表（users、categories、posts、tags、post_tags）
- 插入预设分类（代码、面试八股、感悟）
- 创建默认管理员账户
- 插入示例文章和标签

> **注意：** `init-db` 会清空已有数据并重建所有表。如果只是想重置数据库，直接运行即可；如果已有重要数据，请先备份。

**默认管理员账户：**
- 用户名：`admin`
- 密码：`admin123`

### 3.2 启动后端服务

```bash
cd blog-server
npm install    # 安装依赖（首次运行）
npm start      # 启动服务
```

后端服务将在 `http://localhost:3000` 启动。

### 3.3 启动前端服务

```bash
cd blog-client
npm install    # 安装依赖（首次运行）
npm run dev    # 启动开发服务器
```

前端页面将在 `http://localhost:5173` 可访问。

### 3.4 访问博客

打开浏览器访问 `http://localhost:5173` 即可看到博客首页。

---

## 4. 功能说明

### 4.1 首页

- 展示博客英雄横幅，显示文章总数、浏览量等统计信息
- 分类筛选栏：全部 / 代码 / 面试八股 / 感悟
- 搜索框：支持按标题和摘要搜索
- 文章卡片列表：显示封面、标题、摘要、标签、浏览次数
- 分页导航

### 4.2 文章详情页

- 显示文章标题、分类、作者、发布时间、浏览次数
- Markdown 内容渲染，支持代码语法高亮
- 标签展示
- 返回首页按钮

### 4.3 登录/注册

- 登录页面：输入用户名和密码登录
- 注册页面：输入用户名、密码、昵称（选填）注册
- 登录后可访问后台管理功能
- Token 有效期 7 天

### 4.4 后台管理

访问路径：`/admin`（需要登录）

#### 文章管理
- 查看所有文章列表（含未发布草稿）
- 新建文章：填写标题、分类、摘要、标签、内容
- 编辑文章：修改已有文章的所有字段
- 删除文章：二次确认后删除
- 按分类筛选和关键词搜索

#### 文章编辑器
- 支持 **Markdown** 格式写作（详见第 5 节）
- 三种编辑模式：编辑 / 预览 / 分屏
- 分屏模式下左侧写 Markdown，右侧实时预览
- 标签管理：添加和删除标签
- 发布状态切换：发布 / 草稿
- 封面图设置（URL）

### 4.5 关于页面

- 个人介绍
- 技术栈展示
- 博客板块说明
- 联系方式

---

## 5. Markdown 写作指南

文章编辑器支持标准 Markdown 语法，以下是常用语法速查：

### 标题

使用 `#` 符号表示标题层级，`#` 后需要跟一个空格：

```markdown
# 一级标题
## 二级标题
### 三级标题
#### 四级标题
```

### 文本样式

```markdown
**加粗文字**
*斜体文字*
~~删除线~~
`行内代码`
```

### 列表

```markdown
- 无序列表项1
- 无序列表项2
- 无序列表项3

1. 有序列表项1
2. 有序列表项2
3. 有序列表项3
```

### 代码块

使用三个反引号包裹代码，可指定语言实现语法高亮：

```markdown
​```javascript
function hello() {
  console.log('Hello, World!')
}
​```

​```python
def hello():
    print('Hello, World!')
​```

​```css
.container {
  display: flex;
  justify-content: center;
}
​```
```

支持高亮的语言包括：JavaScript、TypeScript、Python、Java、Go、CSS、HTML、SQL、Shell、Rust 等。

### 引用

```markdown
> 这是一段引用文字
> 可以有多行
```

### 链接和图片

```markdown
[链接文字](https://example.com)
![图片描述](https://example.com/image.png)
```

### 分割线

```markdown
---
```

### 表格

```markdown
| 列1 | 列2 | 列3 |
|-----|-----|-----|
| 数据1 | 数据2 | 数据3 |
```

### 编辑器使用技巧

1. **分屏模式**（推荐）：左侧输入 Markdown，右侧实时预览渲染效果
2. **预览模式**：全屏查看最终渲染效果
3. **编辑模式**：专注于写作，不被预览分心

---

## 6. 技术架构

### 6.1 技术栈

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端框架 | Vue 3 | 渐进式JavaScript框架 |
| UI组件库 | Element Plus | Vue 3组件库 |
| 状态管理 | Pinia | Vue 状态管理方案 |
| 路由 | Vue Router 4 | 官方路由管理器 |
| HTTP客户端 | Axios | Promise-based HTTP客户端 |
| Markdown渲染 | Marked | Markdown解析器（GFM模式） |
| 代码高亮 | highlight.js | 语法高亮库 |
| 构建工具 | Vite 5 | 下一代前端构建工具 |
| 后端框架 | Express | Node.js Web框架 |
| 数据库驱动 | mysql2 | MySQL异步驱动 |
| 认证 | JWT | JSON Web Token |
| 密码加密 | bcryptjs | 密码哈希库 |

### 6.2 数据库结构

```
personal_blog
├── users          - 用户表
├── categories     - 分类表
├── posts          - 文章表（含 category_id, author_id, is_published 等字段）
├── tags           - 标签表
└── post_tags      - 文章-标签关联表
```

### 6.3 API接口

| 方法 | 路径 | 说明 | 认证 |
|------|------|------|------|
| POST | /api/auth/register | 用户注册 | 否 |
| POST | /api/auth/login | 用户登录 | 否 |
| GET | /api/auth/profile | 获取用户信息 | 是 |
| PUT | /api/auth/profile | 更新用户信息 | 是 |
| GET | /api/posts | 获取文章列表 | 否 |
| GET | /api/posts/admin | 获取所有文章(含草稿) | 是 |
| GET | /api/posts/:id | 获取文章详情 | 否 |
| POST | /api/posts | 创建文章 | 是 |
| PUT | /api/posts/:id | 更新文章 | 是 |
| DELETE | /api/posts/:id | 删除文章 | 是 |
| GET | /api/categories | 获取分类列表 | 否 |
| GET | /api/categories/tags | 获取标签列表 | 否 |

### 6.4 项目结构

```
实验五/
├── blog-server/                 # 后端项目
│   ├── config/
│   │   └── db.js               # 数据库连接配置
│   ├── middleware/
│   │   └── auth.js             # JWT认证中间件
│   ├── routes/
│   │   ├── auth.js             # 认证路由
│   │   ├── posts.js            # 文章路由
│   │   └── categories.js       # 分类路由
│   ├── app.js                  # Express应用入口
│   ├── init-db.js              # 数据库初始化脚本（DROP + CREATE）
│   └── package.json
├── blog-client/                 # 前端项目
│   ├── src/
│   │   ├── api/index.js        # API请求封装
│   │   ├── assets/style.css    # 全局样式
│   │   ├── router/index.js     # 路由配置
│   │   ├── stores/auth.js      # 认证状态管理
│   │   ├── views/
│   │   │   ├── Home.vue        # 首页
│   │   │   ├── Login.vue       # 登录/注册页
│   │   │   ├── PostDetail.vue  # 文章详情页（Markdown渲染）
│   │   │   ├── Admin.vue       # 后台管理页（Markdown编辑器）
│   │   │   └── About.vue       # 关于页
│   │   ├── App.vue             # 根组件
│   │   └── main.js             # 入口文件
│   ├── index.html
│   ├── vite.config.js
│   └── package.json
└── 使用手册.md                   # 本文档
```

---

## 7. 常见问题

### Q: 数据库连接失败怎么办？
A: 请检查：
1. MySQL 服务是否已启动
2. 用户名密码是否正确（默认 root / 20061104）
3. MySQL 是否运行在默认端口 3306
4. 数据库 `personal_blog` 是否已创建（运行 `npm run init-db`）

### Q: 启动后接口报错 "Unknown column" 怎么办？
A: 这说明数据库表结构与代码不匹配，请重新运行初始化脚本重建数据库：
```bash
cd blog-server
npm run init-db
```
> 注意：这会清空已有数据。`init-db.js` 使用 DROP + CREATE 策略，确保表结构始终与代码一致。

### Q: 文章中的 `#` 标题不生效怎么办？
A: 确保以下几点：
1. `#` 后面必须跟一个**空格**，如 `## 二级标题`
2. 使用编辑器的**分屏模式**或**预览模式**查看渲染效果（编辑模式显示的是原始 Markdown）
3. 刷新页面重试

### Q: 忘记管理员密码怎么办？
A: 可以重新运行初始化脚本重置，或在数据库中直接修改：
```sql
UPDATE users SET password = '新的bcrypt加密密码' WHERE username = 'admin';
```

### Q: 如何修改数据库配置？
A: 编辑 `blog-server/config/db.js` 文件中的连接参数。

### Q: 如何部署到生产环境？
A:
1. 前端执行 `npm run build` 生成静态文件
2. 将静态文件部署到 Nginx 或其他 Web 服务器
3. 后端使用 PM2 等工具进行进程管理
4. 配置反向代理将 API 请求转发到后端服务

### Q: 如何添加新的文章分类？
A: 直接在数据库的 `categories` 表中插入新记录即可：
```sql
INSERT INTO categories (name, icon, description) VALUES ('新分类', 'Icon', '分类描述');
```

---

**© 2026 个人博客系统 - 前端工程师的技术天地**
