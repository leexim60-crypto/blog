# 博客公网部署指南（Vercel + Render + TiDB Cloud）

部署完成后你将拥有：
- 前端地址：`https://你的项目名.vercel.app`
- 后端地址：`https://你的项目名.onrender.com`
- 数据库：TiDB Cloud（免费 5GB）

---

## 前提：代码上传 GitHub

如果代码已经在 GitHub 上，跳过这步。

在项目根目录 `C:\Users\陈\Desktop\blog` 打开终端：

```bash
git init
git add .
git commit -m "初始化博客项目"
git branch -M main
git remote add origin https://github.com/你的用户名/blog.git
git push -u origin main
```

---

## 第一步：创建免费数据库（TiDB Cloud）

### 1.1 注册并创建集群

1. 打开 https://tidbcloud.com
2. 点 **Get Started Free** → 用 GitHub 或 Google 账号登录
3. 登录后进入控制台，点 **Create Cluster**
4. 选择 **Serverless**（免费）
5. 配置：
   - Cluster Name：`blog`
   - Region：选 **Singapore** 或 **US West**
6. 点 **Create Cluster**
7. 等待约 1 分钟创建完成

### 1.2 设置数据库密码

1. 点集群名进入详情页
2. 左侧菜单点 **Security Settings**
3. 点 **Generate Password** 生成密码
4. **复制并保存这个密码**，只显示一次

### 1.3 获取连接信息

1. 左侧菜单点 **Connect**
2. 你会看到类似这样的信息：

```
Endpoint:  gateway01.us-west-2.prod.aws.tidbcloud.com   ← 记下来
Port:      4000                                           ← 记下来
User:      xxxxxxxx.root                                  ← 记下来
```

### 1.4 创建数据库

1. 左侧菜单点 **SQL Editor**
2. 在编辑框输入：

```sql
CREATE DATABASE personal_blog;
```

3. 点 **Run** 执行

---

## 第二步：部署后端到 Render

### 2.1 注册 Render

1. 打开 https://render.com
2. 点 **Get Started** → **GitHub** 登录
3. 授权 Render 访问你的仓库

### 2.2 创建后端服务

1. 在 Render Dashboard 点 **+ New** → **Web Service**
2. 如果没看到你的仓库，点 **Configure account** 授权
3. 选择你的 `blog` 仓库，点 **Connect**
4. 配置如下：

| 设置项 | 值 |
|--------|-----|
| Name | `blog-server` |
| Region | **Singapore**（和数据库同区域） |
| Runtime | **Docker** |
| Dockerfile Path | `./blog-server/Dockerfile` |
| Instance Type | **Free** |

### 2.3 添加环境变量

点 **Advanced**，添加以下变量：

| Key | Value |
|-----|-------|
| `DB_HOST` | TiDB 的 Endpoint（如 `gateway01.us-west-2.prod.aws.tidbcloud.com`） |
| `DB_PORT` | `4000` |
| `DB_USER` | TiDB 的 User（如 `xxxxxxxx.root`） |
| `DB_PASSWORD` | TiDB 生成的密码 |
| `DB_NAME` | `personal_blog` |
| `DB_SSL` | `true` |
| `JWT_SECRET` | 随便写一串字符，比如 `my_blog_secret_2024` |
| `PORT` | `10000` |

### 2.4 部署

1. 点 **Create Web Service**
2. 等待部署完成（约 3-5 分钟）
3. 看 **Logs** 显示 `博客后端服务已启动: http://localhost:10000` 就成功了
4. 页面顶部会显示你的后端地址，类似 `https://blog-server.onrender.com`
5. **记下这个地址**

### 2.5 验证后端

浏览器打开：`https://你的后端地址/api/health`

应该看到：`{"code":200,"message":"Blog API is running"}`

### 2.6 初始化数据库

在你电脑的终端执行：

```bash
cd C:\Users\陈\Desktop\blog\blog-server
node init-db-remote.js <TiDB的Endpoint> <密码> 4000 true
```

例如：
```bash
node init-db-remote.js gateway01.us-west-2.prod.aws.tidbcloud.com AbCdEf1234 4000 true
```

看到 `数据库初始化完成！` 就成功了。

---

## 第三步：部署前端到 Vercel

### 3.1 注册 Vercel

1. 打开 https://vercel.com
2. 点 **Sign Up** → **Continue with GitHub**

### 3.2 导入项目

1. 点 **Add New...** → **Project**
2. 找到你的 `blog` 仓库，点 **Import**
3. 配置：

| 设置项 | 值 |
|--------|-----|
| Framework Preset | **Vite**（自动识别） |
| Root Directory | 点 **Edit**，改成 `blog-client` |

4. 点 **Environment Variables**，添加：

| Name | Value |
|------|-------|
| `VITE_API_URL` | `https://你的后端地址.onrender.com/api` |

5. 点 **Deploy**

### 3.3 访问博客

部署完成后点击 Vercel 给你的地址即可访问：

| 页面 | 地址 |
|------|------|
| 首页 | `https://xxx.vercel.app` |
| 后台管理 | `https://xxx.vercel.app/admin` |
| 关于页 | `https://xxx.vercel.app/about` |

默认管理员账号：**admin** / **admin123**（登录后建议修改密码）

---

## 后续更新

```bash
git add .
git commit -m "更新说明"
git push
```

Vercel 和 Render 会自动重新部署。

---

## 常见问题

### Q: Render 免费版有什么限制？
A: 15 分钟无人访问会休眠，第一次打开需等 30 秒冷启动。每月 750 小时免费，够用。

### Q: TiDB Cloud 免费吗？
A: Serverless 集群免费 5GB 存储 + 每月 5000 万读取请求，个人博客完全够用。

### Q: 打开页面白屏？
A: 检查 Vercel 的 `VITE_API_URL` 环境变量是否正确。

### Q: 登录报错"网络错误"？
A: 浏览器打开 `https://后端地址/api/health` 确认后端正常运行。

### Q: 数据库连接失败？
A: 确认环境变量中 `DB_SSL=true` 已设置，TiDB Cloud 必须用 SSL 连接。

### Q: Render 部署失败？
A: 点 **Logs** 看错误。常见原因：Dockerfile Path 不对（必须是 `./blog-server/Dockerfile`）。
