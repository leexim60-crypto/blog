# 博客公网部署指南

部署完成后你将拥有：
- 前端地址：`https://你的项目名.vercel.app`
- 后端地址：`https://你的项目名.up.railway.app`

---

## 前提：把代码上传到 GitHub

如果代码已经在 GitHub 上，跳过这步。

### 1.1 创建 GitHub 仓库

1. 打开 https://github.com ，登录
2. 点右上角 **+** → **New repository**
3. Repository name 填 `blog`
4. 选 **Private**（私有仓库）
5. **不要**勾选任何初始化选项（不要 README、不要 .gitignore）
6. 点 **Create repository**

### 1.2 上传代码

在项目根目录 `C:\Users\陈\Desktop\blog` 打开终端，执行：

```bash
git init
git add .
git commit -m "初始化博客项目"
git branch -M main
git remote add origin https://github.com/你的用户名/blog.git
git push -u origin main
```

---

## 第一步：部署后端到 Railway

Railway 提供免费额度，包含 MySQL 数据库和后端托管。

### 1.1 注册 Railway

1. 打开 https://railway.app
2. 点 **Login** → **Login with GitHub**
3. 授权 Railway 访问你的 GitHub

### 1.2 创建 MySQL 数据库

1. 进入 Railway 首页，点 **New Project**
2. 点 **Provision MySQL**
3. 等待几秒，MySQL 服务创建完成
4. 点击 **MySQL** 服务卡片
5. 点顶部的 **Connect** 标签
6. 你会看到类似这样的信息，**全部记下来**：

```
Host:   mysql-xxx.railway.internal    ← 复制这个
Port:   3306
User:   root
Password: AbCdEfGh1234...            ← 复制这个
Database: railway
```

> 注意：如果你看到的是 `.internal` 结尾的地址，点旁边的 **Public** 按钮切换成公网地址（类似 `roundhouse.proxy.rlwy.net`）

### 1.3 部署后端服务

1. 回到 Railway 首页你的项目页面
2. 点 **+ New** → **GitHub Repo**
3. 如果第一次使用，点 **Configure GitHub App** 授权仓库访问
4. 选择你的 `blog` 仓库
5. Railway 会问你要部署哪个目录，选 **`blog-server`**
6. 点 **Deploy Now**

### 1.4 配置后端环境变量

1. 点击刚创建的 **blog-server** 服务卡片
2. 点顶部的 **Variables** 标签
3. 点 **New Variable**，逐个添加以下变量：

| Variable Name | Value |
|---------------|-------|
| `DB_HOST` | 上面记的 MySQL Host（公网地址） |
| `DB_USER` | `root` |
| `DB_PASSWORD` | 上面记的 MySQL Password |
| `DB_NAME` | `railway` |
| `JWT_SECRET` | 随便输入一串字符，比如 `my_blog_secret_2024` |

4. 添加完后 Railway 会自动重新部署

### 1.5 获取后端地址

1. 等待部署完成（状态变绿），大约 1-2 分钟
2. 点 **Settings** 标签
3. 找到 **Public Networking** 或 **Networking**
4. 点 **Generate Domain**
5. 你会得到一个地址，类似：`https://blog-server-production-xxxx.up.railway.app`
6. **记下这个地址**，后面要用

### 1.6 验证后端

在浏览器打开：`https://你的后端地址/api/health`

应该看到：
```json
{"code":200,"message":"Blog API is running"}
```

### 1.7 初始化数据库

在你电脑的终端执行（把地址和密码换成你的）：

```bash
cd C:\Users\陈\Desktop\blog\blog-server

DB_HOST=你的MySQL公网地址 DB_PASSWORD=你的密码 DB_NAME=railway node init-db.js
```

看到 `数据库初始化完成！` 就成功了。

> Windows 用户如果上面的命令不行，可以创建一个临时 `.env` 文件：
> ```
> DB_HOST=你的MySQL公网地址
> DB_PASSWORD=你的密码
> DB_NAME=railway
> ```
> 然后执行 `node init-db.js`，完成后删除 `.env` 文件。

---

## 第二步：部署前端到 Vercel

### 2.1 注册 Vercel

1. 打开 https://vercel.com
2. 点 **Sign Up** → **Continue with GitHub**
3. 授权

### 2.2 导入项目

1. 进入 Vercel 首页，点 **Add New...** → **Project**
2. 找到你的 `blog` 仓库，点 **Import**
3. 配置如下：

| 设置项 | 值 |
|--------|-----|
| Framework Preset | **Vite**（应该自动识别） |
| Root Directory | 点 **Edit**，改成 `blog-client` |

4. 点 **Environment Variables**，添加：

| Name | Value |
|------|-------|
| `VITE_API_URL` | `https://你的后端地址/api`（第一步记的后端地址 + `/api`） |

5. 点 **Deploy**

### 2.3 等待部署完成

大约 1-2 分钟，部署成功后会显示一个地址。

### 2.4 访问你的博客

点击 Vercel 给你的地址，类似：`https://blog-xxx.vercel.app`

你应该能看到博客首页了！

- 首页：`https://blog-xxx.vercel.app`
- 后台管理：`https://blog-xxx.vercel.app/admin`（用 admin / admin123 登录）
- 关于页：`https://blog-xxx.vercel.app/about`

---

## 后续更新代码

以后改了代码想更新线上版本：

```bash
git add .
git commit -m "更新了xxx"
git push
```

- **Vercel** 会自动检测到 GitHub 更新，自动重新部署前端
- **Railway** 也会自动检测到更新，自动重新部署后端

---

## 常见问题

### Q: 打开页面白屏？
A: 检查 Vercel 的 `VITE_API_URL` 环境变量是否正确，必须是 `https://后端地址/api`

### Q: 登录报错"网络错误"？
A: 检查后端是否正常运行，浏览器打开 `https://后端地址/api/health` 看看有没有响应

### Q: Railway 显示部署失败？
A: 点 **Deployments** → 点失败的那次 → 看 **Build Logs** 报什么错。常见原因是环境变量没配对

### Q: 数据库连接失败？
A: 确认 Railway MySQL 的 Host 是公网地址（不是 `.internal` 结尾的），密码是对的

### Q: 免费额度够用吗？
A: Railway 每月有 $5 免费额度，个人博客完全够用。Vercel 免费额度也很充裕

### Q: 想用自定义域名？
A: Vercel → 项目 → Settings → Domains → 添加你的域名，按提示配置 DNS
